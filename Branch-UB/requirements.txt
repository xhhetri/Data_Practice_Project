# --- Actually imported by src/analysis/*, run_pipeline.py, and tests/ ---
pandas>=2.2
numpy>=1.26
matplotlib>=3.10
scikit-learn>=1.4
statsmodels>=0.14

# --- Dev / testing ---
pytest>=8.0

# --- Anticipated, not yet imported by any current module ---
# Uncomment when needed:
# openpyxl>=3.1   # real government source files (petroleum stats, NGA factors) ship as .xlsx
# requests>=2.31  # only needed once a live-download script is written; clean.py
                  # currently reads local files only, no HTTP calls
